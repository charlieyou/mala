{
  "description": "python -c with write hint but no literal target",
  "tool_name": "bash",
  "tool_input": {
    "command": "python -c \"import os; open(os.environ['F'],'w').write('x')\""
  },
  "expected": {
    "hookSpecificOutput": {
      "hookEventName": "PreToolUse",
      "permissionDecision": "deny",
      "permissionDecisionReason": "path '<unresolved-write-target>' has no active lock for agent 'agent-me'"
    }
  }
}
