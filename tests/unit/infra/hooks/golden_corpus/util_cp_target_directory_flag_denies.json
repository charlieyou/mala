{
  "description": "cp -t DIR src \u2014 destination supplied via -t flag",
  "tool_name": "bash",
  "tool_input": {
    "command": "cp -t {repo}/dstdir /etc/hosts"
  },
  "expected": {
    "hookSpecificOutput": {
      "hookEventName": "PreToolUse",
      "permissionDecision": "deny",
      "permissionDecisionReason": "path '{repo}/dstdir' has no active lock for agent 'agent-me'"
    }
  }
}
