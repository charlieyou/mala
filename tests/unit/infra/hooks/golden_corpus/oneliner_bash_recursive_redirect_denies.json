{
  "description": "bash -c 'echo x > file' recursively re-applies the heuristic",
  "tool_name": "bash",
  "tool_input": {
    "command": "bash -c 'echo x > {repo}/f.txt'"
  },
  "expected": {
    "hookSpecificOutput": {
      "hookEventName": "PreToolUse",
      "permissionDecision": "deny",
      "permissionDecisionReason": "path '{repo}/f.txt' has no active lock for agent 'agent-me'"
    }
  }
}
