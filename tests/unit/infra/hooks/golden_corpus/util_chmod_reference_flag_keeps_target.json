{
  "description": "chmod --reference=R target \u2014 target is the only positional",
  "tool_name": "bash",
  "tool_input": {
    "command": "chmod --reference=/etc/hosts {repo}/dst.txt"
  },
  "expected": {
    "hookSpecificOutput": {
      "hookEventName": "PreToolUse",
      "permissionDecision": "deny",
      "permissionDecisionReason": "path '{repo}/dst.txt' has no active lock for agent 'agent-me'"
    }
  }
}
