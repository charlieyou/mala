{
  "description": "sed -i.bak attached-suffix unlocked",
  "tool_name": "bash",
  "tool_input": {
    "command": "sed -i.bak 's/x/y/' {repo}/file.py"
  },
  "expected": {
    "hookSpecificOutput": {
      "hookEventName": "PreToolUse",
      "permissionDecision": "deny",
      "permissionDecisionReason": "path '{repo}/file.py' has no active lock for agent 'agent-me'"
    }
  }
}
