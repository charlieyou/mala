{
  "description": "`>& file` legacy combined-redirect form",
  "tool_name": "bash",
  "tool_input": {
    "command": "ls >& {repo}/all.log"
  },
  "expected": {
    "hookSpecificOutput": {
      "hookEventName": "PreToolUse",
      "permissionDecision": "deny",
      "permissionDecisionReason": "path '{repo}/all.log' has no active lock for agent 'agent-me'"
    }
  }
}
