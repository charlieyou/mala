{
  "description": "`<>` POSIX read-write open creates file",
  "tool_name": "bash",
  "tool_input": {
    "command": "exec 3<> {repo}/rw.log"
  },
  "expected": {
    "hookSpecificOutput": {
      "hookEventName": "PreToolUse",
      "permissionDecision": "deny",
      "permissionDecisionReason": "path '{repo}/rw.log' has no active lock for agent 'agent-me'"
    }
  }
}
