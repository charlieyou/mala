{
  "description": "git -C subdir checkout file \u2192 lock-check against subdir/file",
  "tool_name": "bash",
  "tool_input": {
    "command": "git -C subdir checkout f.py"
  },
  "expected": {
    "hookSpecificOutput": {
      "hookEventName": "PreToolUse",
      "permissionDecision": "deny",
      "permissionDecisionReason": "path 'subdir/f.py' has no active lock for agent 'agent-me'"
    }
  }
}
