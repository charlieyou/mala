{
  "description": "python -c Path(lit).write_text",
  "tool_name": "bash",
  "tool_input": {
    "command": "python -c \"from pathlib import Path; Path('{repo}/f.py').write_text('x')\""
  },
  "expected": {
    "hookSpecificOutput": {
      "hookEventName": "PreToolUse",
      "permissionDecision": "deny",
      "permissionDecisionReason": "path '{repo}/f.py' has no active lock for agent 'agent-me'"
    }
  }
}
