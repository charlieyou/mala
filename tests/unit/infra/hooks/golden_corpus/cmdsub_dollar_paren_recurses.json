{
  "description": "echo $(touch f) \u2192 recursive cmdsub body extract",
  "tool_name": "bash",
  "tool_input": {
    "command": "echo $(touch {repo}/f.txt)"
  },
  "expected": {
    "hookSpecificOutput": {
      "hookEventName": "PreToolUse",
      "permissionDecision": "deny",
      "permissionDecisionReason": "path '{repo}/f.txt' has no active lock for agent 'agent-me'"
    }
  }
}
