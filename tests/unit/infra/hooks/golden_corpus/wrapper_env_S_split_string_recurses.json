{
  "description": "env -S 'touch f' \u2192 recursively process the body",
  "tool_name": "bash",
  "tool_input": {
    "command": "env -S 'touch {repo}/f.txt'"
  },
  "expected": {
    "hookSpecificOutput": {
      "hookEventName": "PreToolUse",
      "permissionDecision": "deny",
      "permissionDecisionReason": "path '{repo}/f.txt' has no active lock for agent 'agent-me'"
    }
  }
}
