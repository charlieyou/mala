{
  "description": "tee target unlocked",
  "tool_name": "bash",
  "tool_input": {
    "command": "echo x | tee {repo}/out.log"
  },
  "expected": {
    "hookSpecificOutput": {
      "hookEventName": "PreToolUse",
      "permissionDecision": "deny",
      "permissionDecisionReason": "path '{repo}/out.log' has no active lock for agent 'agent-me'"
    }
  }
}
