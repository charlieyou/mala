{
  "description": "Glob char in extracted target",
  "tool_name": "bash",
  "tool_input": {
    "command": "echo hi > {repo}/*.log"
  },
  "expected": {
    "hookSpecificOutput": {
      "hookEventName": "PreToolUse",
      "permissionDecision": "deny",
      "permissionDecisionReason": "path '{repo}/*.log' has no active lock for agent 'agent-me'"
    }
  }
}
