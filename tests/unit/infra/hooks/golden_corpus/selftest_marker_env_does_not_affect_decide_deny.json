{
  "description": "SELFTEST_MARKER_ENV set + bash write \u2192 decide() applies normal write-path gate",
  "tool_name": "bash",
  "tool_input": {
    "command": "echo hi > {repo}/f.log"
  },
  "env": {
    "MALA_CODEX_HOOK_SELFTEST_MARKER": "{repo}/marker.json"
  },
  "expected": {
    "hookSpecificOutput": {
      "hookEventName": "PreToolUse",
      "permissionDecision": "deny",
      "permissionDecisionReason": "path '{repo}/f.log' has no active lock for agent 'agent-me'"
    }
  }
}
