{
  "description": "gawk --include=inplace target unlocked",
  "tool_name": "bash",
  "tool_input": {
    "command": "gawk --include=inplace '{print}' {repo}/f.py"
  },
  "expected": {
    "hookSpecificOutput": {
      "hookEventName": "PreToolUse",
      "permissionDecision": "deny",
      "permissionDecisionReason": "path '{repo}/f.py' has no active lock for agent 'agent-me'"
    }
  }
}
