{
  "description": "ln src (single positional) lock-checks `./basename(src)`",
  "tool_name": "bash",
  "tool_input": {
    "command": "ln /tmp/srcname.txt"
  },
  "cwd": "{repo}",
  "expected": {
    "hookSpecificOutput": {
      "hookEventName": "PreToolUse",
      "permissionDecision": "deny",
      "permissionDecisionReason": "path 'srcname.txt' has no active lock for agent 'agent-me'"
    }
  }
}
