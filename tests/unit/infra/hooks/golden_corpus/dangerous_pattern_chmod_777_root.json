{
  "description": "DANGEROUS_PATTERNS hit: chmod -R 777 /",
  "tool_name": "bash",
  "tool_input": {
    "command": "chmod -R 777 /etc"
  },
  "expected": {
    "hookSpecificOutput": {
      "hookEventName": "PreToolUse",
      "permissionDecision": "deny",
      "permissionDecisionReason": "command matches dangerous pattern: chmod -R 777 /"
    }
  }
}
