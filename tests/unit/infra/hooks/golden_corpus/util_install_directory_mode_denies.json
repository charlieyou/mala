{
  "description": "install -d DIR1 DIR2 \u2014 every positional is a directory write",
  "tool_name": "bash",
  "tool_input": {
    "command": "install -d {repo}/d1 {repo}/d2"
  },
  "locks": [
    {
      "path": "{repo}/d1",
      "agent_id": "agent-me"
    }
  ],
  "expected": {
    "hookSpecificOutput": {
      "hookEventName": "PreToolUse",
      "permissionDecision": "deny",
      "permissionDecisionReason": "path '{repo}/d2' has no active lock for agent 'agent-me'"
    }
  }
}
