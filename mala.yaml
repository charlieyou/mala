preset: python-uv

run_level_commands:
  test: "uv run pytest -m 'unit or integration or e2e' --cov=src --cov-report=xml:coverage.xml --cov-fail-under=0 -o cache_dir=/tmp/pytest-${AGENT_ID:-default}"

coverage:
  format: xml
  file: coverage.xml
  threshold: 72
